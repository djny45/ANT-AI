ant:
  name: ANT AI
  version: 0.3
  mode: personal

models:
  selection: user-profile
  routing: explicit
  aggregator: disabled

security:
  encryption: enabled
  audit_ledger: enabled
  rate_limit: enabled

swarm:
  enabled: true
  agents:
    - research_ant
    - coding_ant
    - security_ant
